Do not write any code until I instruct you to "write elegant code" in a future step.

To start, please read CLAUDE.md, if you haven't already.

Before we continue with to the planning stage: dig in, explore the codebase, read relevant files, and prepare to discuss the ins and outs of how it works. Exclude node_modules and paths listed in .gitignore. View the app at http://localhost:3000 (web) or http://localhost:5173 (widget) using MCP Playwright and take a snapshot for your contextual awareness.