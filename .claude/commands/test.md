Run all lint checks, build checks, type checks and unit tests. Be sure to ultrathink and run these in one-time mode and not watch mode, so you don't get stuck. Be sure to run lint checks on both the API and UI. Write elegant code to correct any errors and critical warnings you find. Keep going until they're all resolved.

When you're done, do one final ultrathink pass of all these tests again. Load up the web app in the browser using MCP Playwright and check for critical console errors. Make any final corrections as needed.

$ARGUMENTS