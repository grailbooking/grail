Run the API and UI dev servers in the background so that you can read the logs and catch any errors.

First, kill any running processes using the default ports for the API and UI dev servers, including 3000, 3001, 5173, 5174, 8000, 8001.

Then, run the API and UI dev servers in the background.

$ARGUMENTS